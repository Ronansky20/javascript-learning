Character Sheet Display (3.1)
    TODO: Show all 6 stats with modifiers (1.1, 1.2) <- This is already done
    TODO: HP bar, level, class <- make a div that shows the level, a div that shows hp / maxHp
Inventory System (1.3)
    TODO: Add/remove items <- inventory.js, two input fields, one for item name and the other for weight. Then button with "Add item"
    TODO: Track gold <- div showing gold: amount currently owned
    TODO: Weight limit <- amount of weght in inventory and what is still available
    TODO: Dice Roller (2.2, 2.3, 3.2) <- dice tray with buttons for commonly used dice combinations
    TODO: Click stats to roll checks <- already exists
    TODO: Roll attacks with damage (2.3) <- attackSystem.js, attackBonus and damageBonus comes from character stats and equipment, give standard AC before making an encounter creator with random monsters from API
    TODO: Show roll history <- already exists
    TODO: Advantage/disadvantage option <- see 2.2 task 4 (diceRoller.js)
Save/Load (3.3)
    TODO: Save character to localStorage <- already exists
    TODO: Load on page refresh <- already exsist
    TODO: Manage multiple characters <- dropdown of current keys which corresponds to current characters
Bonus Features:
    [ ]: Fetch random monsters for encounters (2.4) <- pull from api, display specific stats. Use those to make encounters.
    [ ]: Party manager to track multiple PCs (1.4) <- partyManager.js?
    [ ]: Spell slots tracker <- Make spellslots?